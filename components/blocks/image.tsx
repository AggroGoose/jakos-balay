import { ImgBlock } from "@/lib/blog";

export default function ImageBlock({ block }: { block: ImgBlock }) {
  // eslint-disable-next-line @next/next/no-img-element
  return <img alt="placeholder"></img>;
}
